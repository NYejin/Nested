# Nested — 구현 기능 정리

도메인별로 구현된 기능과 상태를 정리한 문서입니다.

**상태 표기**
- ✅ 완료 — 실제 API 연동, 동작 확인
- 🔴 미구현 — 다음 단계 후보

---

## A. 매칭/추천

### A-1. 성향 데이터
| 기능 | 설명 | 상태 |
|---|---|---|
| 성향 데이터 스키마 | `RoommatePreference` 모델 (9개 축) | ✅ |
| 성향 설문/수정 | `GET/PUT /me/preference` + `/me/preference` 페이지 | ✅ |

### A-2. 룸메이트 매칭
| 기능 | 설명 | 상태 |
|---|---|---|
| 매칭 알고리즘 | `scoreMatch()` — 9축 균등 가중치(각 1/9) + 3축(흡연/반려동물/방문객) 하드필터 | ✅ |
| 매칭 화면 | `/match` 페이지, `GET /match`, `GET /match/:userId` | ✅ |
| 매칭 이유 표시 | 일치 사유 / 조율 필요 사유를 자연어로 표시 | ✅ |
| 성별 상호 매칭 필터 | `isMutuallyGenderCompatible()` | ✅ |

> **알고리즘 스펙:** 9개 축 전부 동일 가중치. `smoking`/`pets`/`visitors` 3축은 답변 차이가 2단계(완전 반대)면 나머지 궁합과 무관하게 매칭이 차단되는 하드필터. 나머지 6축은 점수에만 반영(gap 0→1점, gap 1→0.5점, gap 2→0점). 최종 점수 = round((총점 / 9) × 100).

### A-3. 숙소 추천
| 기능 | 설명 | 상태 |
|---|---|---|
| 유사 숙소 추천 | `findSimilar()` — 지역·방종류·가격·성별정책·편의시설 겹침을 점수화, `GET /rooms/:id/similar` | ✅ |
| 추천 이유 표시 | 유사 숙소 추천에 부속 — 겹치는 속성을 자연어 reasons로 반환 | ✅ |
| 개인화 숙소 추천 | 성향·검색 이력 기반 추천 | 🔴 |

---

## B. 회원 / 프로필

### B-1. 인증
로컬/소셜(Google·Kakao·Naver·Apple) 회원가입·로그인, JWT access/refresh 회전, 자동 로그인/로그아웃, `GET /auth/me` — 전체 ✅

### B-2. 프로필
| 기능 | 설명 | 상태 |
|---|---|---|
| 프로필 수정 | `PATCH /auth/me` | ✅ |
| 비밀번호 변경/찾기 | `POST /auth/change-password`, `/forgot-password`, `/reset-password` | ✅ |
| 회원 탈퇴 | `DELETE /auth/me` | ✅ |
| 설정 페이지 | 비밀번호 변경·회원 탈퇴·호스트 권한 포기 실제 API 연결 | ✅ |
| 호스트 권한 포기 | 확인 문구 입력 후 호스트→게스트 전환, 예약·리뷰 없는 숙소는 삭제·있는 숙소는 비공개 전환 | ✅ |

### B-3. 알림
알림 조회/읽음/삭제, 실시간 알림(`notifications.gateway.ts` + Socket.io), 알림 클릭 이동 — 전체 ✅

### B-4. 마이페이지
내 프로필, 리뷰 목록(작성/받은 리뷰 분리), 뱃지 시스템 — 전체 ✅

---

## C. 주거 등록 / 관리

### C-1~2. 숙소 등록 · 호스트 기능
| 기능 | 설명 | 상태 |
|---|---|---|
| 숙소 등록/수정/삭제 | 사진 업로드 포함 | ✅ |
| 예약 관리 | 승인/거절/노쇼/조기퇴실/연장/입주자 리뷰 | ✅ |
| 호스트 대시보드 · 캘린더 | 예약 불가일 블록 설정 | ✅ |
| 수익 CSV 내보내기 | `GET /host/export/{revenue,tenants}.csv` | ✅ |
| 연체·정산 관리 | `GET /host/settlements` | ✅ |

### C-3. 관리자
숙소 승인/거부, 회원 관리(정지/역할변경), 신고 관리, 통계·매출 차트, 공지/배너 CRUD, 쿠폰 관리, 휴지통(게시글/댓글 복원) — 전체 ✅

---

## D. 검색 / 상호작용

### D-1. 검색/탐색
검색·상세·지도·찜하기, 정렬/페이지네이션 — 전체 ✅

### D-2. 예약/결제
예약 견적/생성/취소(`quote → create → confirm`), 결제 승인, 내 여행 목록, 결제 내역, 쿠폰 발급/관리 — 전체 ✅

### D-3. 메시지/커뮤니티
채팅방 개설, 메시지 목록/전송(이미지 포함), 실시간 메시지(Socket.io), 읽음 처리, 커뮤니티 글/댓글 — 전체 ✅

---

## 기타 도메인

| 모듈 | 설명 | 상태 |
|---|---|---|
| 친구 (`friends`) | 친구 요청/수락/거절 | ✅ |
| 문의 (`inquiries`) | 고객센터 문의 접수/답변 | ✅ |
| 이동수단 (`transit`) | 다중 이동수단 정보 API | ✅ |
| 세입자 리뷰 (`tenant-review`) | 세입자 리뷰 + 뱃지 연동 | ✅ |
| 공개 프로필 (`users`) | `GET /users/:id` | ✅ |
| 파일 업로드 (`storage`) | Cloudinary 서명 업로드, presign 삭제 | ✅ |
